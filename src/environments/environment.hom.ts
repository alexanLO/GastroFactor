// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --configuration homologation` replaces `environment.ts` with this file.
export const environment = {
  production: false,
  baseAddress: '',
};
